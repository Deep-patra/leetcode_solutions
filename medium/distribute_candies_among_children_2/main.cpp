#include <bits/stdc++.h>
using namespace std;

using long long ll;

// Stars and Bars technique
ll distribute_candies(int n, int limit) {

  

  ll res = 0;

  return res;
}

int main() {
  int t;
  cin >> t;

  while (t > 0) {
    t --;

    int n, limit;
    cin >> n >> limit;

    ll res = distribute_candies(n, limit);
    cout << res << '\n';
  }

  return 0;
}
