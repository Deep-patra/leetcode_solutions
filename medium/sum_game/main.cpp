#include <bits/stdc++.h>  
using namespace std;

bool sum_game(string num) {
  
}

int main() {
  int t;
  cin >> t;

  while (t > 0) {
    t --;

    string num;
    cin >> num;

    bool res = sum_game(num);
    cout << res << endl;
  }

  return 0;
}
