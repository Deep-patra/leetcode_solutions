#include <bits/stdc++.h>
using namespace std;

// TODO: Complete the implementation
int find_nth_digit(int n) {}

int main() {
  int t;
  cin >> t;

  while (t > 0) {
    t--;

    int n;
    cin >> n;

    int result = find_nth_digit(n);
    cout << result << '\n';
  }

  return 0;
}
