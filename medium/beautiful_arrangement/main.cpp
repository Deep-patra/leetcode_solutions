#include <bits/stdc++.h>
using namespace std;

// Bitmask DP
int count_arrangement(int n) {}

int main() {
  int t;
  cin >> t;

  while (t > 0) {
    t--;

    int n;
    cin >> n;

    int res = count_arrangement(n);
    cout << res << endl;
  }

  return 0;
}
