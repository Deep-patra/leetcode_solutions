#include <iostream>
#include <vector>
using namespace std;

vector<vector<int>> fourSum(vector<int> &nums, int target) {}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  return 0;
}
