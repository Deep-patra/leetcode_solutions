#include <bits/stdc++.h>  
using namespace std;

string remove_duplicate_letters(string s) {

}

int main() {
  int t;
  cin >> t;

  while (t > 0) {
    t --;

    string s;
    cin >> s;

    string res = remove_duplicate_letters(s);
    cout << res << endl;
  }

  return 0;
}
