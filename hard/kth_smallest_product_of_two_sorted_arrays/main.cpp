#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

// INFO: Use Binary Search Approach
ll kth_smallest_product(vector<int> &nums1, vector<int> &nums2, ll k) {
  
}

int main() {
  int t;
  cin >> t;

  while (t > 0) {
    t --;

    int n, m, k;
    cin >> n >> m >> k;

    vector<int> nums1(n), nums2(m);

    for (int i = 0; i < n; i ++)
      cin >> nums1[i];


    for (int j = 0; j < m; j ++)
      cin >> nums2[j];
  }

  return 0;
}
